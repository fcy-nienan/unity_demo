using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlaneMouse : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        // if (Input.GetKey("w"))
        // {
        //     Debug.Log("i also get key w!");
        // }
        // else if (Input.GetKey("s"))
        // {
        //     Debug.Log("i also get key s!");
        // }
        //
        // if (Input.GetKey("a"))
        // {
        //     Debug.Log("i also get key a!");
        // }
        // else if (Input.GetKey("d"))
        // {
        //     Debug.Log("i also get key d!");
        // }
    }
}
